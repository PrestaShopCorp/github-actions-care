name: "PHP Syntax Checker"
author: Support Tech Care
inputs:
  php-version:
    description: "PHP version to use"
    required: false

runs:
  using: "composite"
  steps:
    - name: PHP syntax checker -${{ inputs.php-version }}-
      